# Optional: when set, /api/chat uses OpenAI instead of the in-process mock.
OPENAI_API_KEY=

# Required for live/deploy. Local mock may omit this (uses a labeled demo fallback).
# Generate a stable secret (e.g. openssl rand -base64 32) and keep it fixed across deploys.
TAILRACE_VAULT_KEY=
