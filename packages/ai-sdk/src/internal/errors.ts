/**
 * PolicyViolationError translation for AI SDK surfaces.
 * Tool block wording lives in `@tailrace/adapter` (shared with other hosts).
 */

export { formatToolBlockError, PolicyViolationError } from "@tailrace/adapter";
