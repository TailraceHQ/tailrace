---
"@tailrace/cloud": minor
"@tailrace/core": patch
---

Add `@tailrace/cloud` (remotePolicy + remoteAuditSink) for the hosted policy plane client, and export `defaultPolicy` from core for fail-open cold-start fallback.
